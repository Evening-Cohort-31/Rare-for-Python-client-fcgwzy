import "./button.css";

export const ActivationButton = ({ onClick }) => {
    return (
        <button>Activate User</button>
    )
}

export const DeactivationButton = ({ onClick }) => {
    return (
        <button>Deactivate User</button>
    )
}